# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Python bytecode from the verification scripts
__pycache__/
*.py[cod]

# book-forge build cache (cover page, staged PDFs)
.bf-cache/
*.staged
*.new.pdf

# The build pipeline now lives in book-forge, pinned per book, rather than
# git-ignored here. `build/` is the retired local copy, kept until the
# duplicate clone is retired; it is not the source of truth any more.
build/
